function abbreviateString(str) {
    if(typeof(str) !== 'string')
      throw new Error('')
    str = str.split(' ')
    return str[0] + ' ' + str[str.length-1][0].toUpperCase() + '.' 
}

export { abbreviateString };
